# ─────────────────────────────────────────────────────────────
# Kisan Sahayak — Python dependencies
# Install:  pip install -r requirements.txt
#           playwright install chromium
# After a clean build, pin exact versions:  pip freeze > requirements.txt
# ─────────────────────────────────────────────────────────────

# ── Orchestration (LangChain — LCEL, no LangGraph) ──
langchain
langchain-core
langchain-community
langchain-openai
openai                      # SDK; also used for Sarvam via base_url

# ── Retrieval / vector store ──
faiss-cpu
rank-bm25

# ── Re-ranker (local, GPU-capable) ──
FlagEmbedding               # bge-reranker-v2-m3
# sentence-transformers     # alternative reranker backend

# ── Ingestion / loaders ──
pymupdf                     # PDF (text)
beautifulsoup4              # HTML
lxml
playwright                  # web scrape → file (offline, one-time)

# ── UI ──
streamlit
streamlit-mic-recorder      # voice capture in the browser

# ── Utilities ──
python-dotenv               # load .env
langdetect                  # backup to Sarvam /detect-language
requests
tqdm
